<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.dao.PmsStationDao">

    <select id="list" resultMap="com.macro.mall.mapper.PmsStationMapper.BaseResultMap">
    select st.*
    from pms_station st
    where id>0
    <if test="status!=null">
           AND status=#{status} 
    </if>
    <if test="address!=null">
           AND (city like concat("%",#{address},"%") or 
            province like concat("%",#{address},"%") or 
            region like concat("%",#{address},"%") or 
            detail_address like concat("%",#{address},"%")
           )
    </if>
    </select>
</mapper>